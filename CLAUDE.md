# 记账APP (Bookkeeping App)

## 项目概述
- 产品名称：记账APP
- 平台：macOS 桌面应用
- 定位：简单易用的个人记账工具，记录每一笔人民币花销，通过分类了解「钱花在哪了」
- 目标用户：个人用户，追求简单快捷的记录体验

## 核心功能（第一版 MVP）
1. 记一笔：选择「支出 / 收入」、输入金额（元）、选二级分类、日期（默认今天）、可选备注
2. 二级分类体系：支出与收入各自一套「一级大类 + 二级小类」，内置预设分类，可自定义
3. 看流水：按日期查看账单明细，支出/收入分色显示，每天显示支出与收入小计
4. 统计：本月支出 / 本月收入 / 本月结余 / 记账笔数
5. 本地存储：数据保存在用户自己的电脑上，不上传云端

## 分类设计（二级）
| 一级大类 | 二级小类 |
|---------|---------|
| 餐饮 | 早餐 / 午餐 / 晚餐 / 外卖 / 零食 / 咖啡茶饮 / 聚餐 |
| 交通 | 公交地铁 / 打车 / 加油 / 停车 / 高铁火车 / 机票 / 共享单车 |
| 购物 | 服饰鞋包 / 日用品 / 数码家电 / 美妆护肤 / 母婴 / 其他 |
| 居住 | 房租 / 水电燃气 / 物业 / 维修 / 宽带 |
| 娱乐 | 电影 / 游戏 / 会员订阅 / 旅游 / 运动健身 |
| 医疗 | 药品 / 门诊 / 体检 / 保健 |
| 教育 | 书籍 / 课程培训 / 考试报名 |
| 人情往来 | 红包礼金 / 请客送礼 |
| 通讯 | 话费 / 流量 / 快递 |
| 其他 | 其他 |

## 技术栈（已选定）
- 桌面框架：Electron
- 前端框架：Vue 3 + Vite
- UI 组件库：Element Plus（用户已确认）
- 数据存储：JSON 文件（保存在用户数据目录，由主进程通过 IPC 读写，用户已确认）

## ⚠️ 重要协作约定（本项目全程遵守）
用户是编程小白、首次使用 Claude Code，无法提供具体技术要求。因此：
1. 任何「技术相关」的决策，Claude 必须先列出多个方案；
2. 用通俗易懂的语言向用户解释每个方案的优劣势；
3. 给出推荐，但最终决定权在用户；
4. 未经用户确认，不得擅自替用户做技术选型或关键实现决策。

## 开发路线图
- [x] 产品文档与分类设计
- [x] 技术方案选定（Electron + Vue 3 + Vite）
- [x] 项目脚手架搭建（electron-vite 结构）
- [x] 记账功能（新增 / 编辑 / 删除，含二级分类选择）
- [x] 流水查看（按日期分组、每日小计、今日/本月统计卡片）
- [x] 分类管理（自定义增删一级/二级分类、改名/换图标；分类下有记录时禁止删除；改名后已记的账自动同步）
- [ ] 打包成 Mac App（.app / .dmg）

## 数据模型（data.json）
```json
{
  "records": [
    { "id", "type": "expense|income", "amount", "category", "subcategory", "date": "YYYY-MM-DD", "note", "createdAt" }
  ],
  "categories": {
    "expense": [ { "name": "餐饮", "emoji": "🍜", "children": ["早餐", "..."] } ],
    "income":  [ { "name": "工资", "emoji": "💰", "children": ["基本工资", "..."] } ]
  }
}
```
- 每笔账带 `type`（expense 支出 / income 收入），收支分开统计。
- records 存分类名（字符串）。改分类名时主进程会级联更新 records（按 type 区分），保证不产生「孤儿分类」。
- 老数据迁移：records 缺 type 补 `expense`；categories 是数组时迁移为 `{ expense: 原数组, income: 默认收入分类 }`。

## 已知环境注意事项
- 在 VS Code 里运行 `npm run dev` 时，必须清空 `ELECTRON_RUN_AS_NODE` 环境变量（VS Code 自身是 Electron，会设置该变量导致 App 无法启动）。已在 package.json 的 dev 脚本中处理：`ELECTRON_RUN_AS_NODE= electron-vite dev`。
- 新版 npm（11+）默认拦截 install 脚本，需 `npm approve-scripts electron esbuild` 批准后 Electron 二进制才能下载。已批准并写入 package.json 的 `allowScripts`。
